apiVersion: v1
kind: Service
metadata:
    name: "kubet-c1cd"
    labels:
        app: "kubet-c1cd"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubet-c1cd"